package defau;

public class Program {
	public static void main(String[] args) {

		int min = Integar.MAX_VALUE;
		int max = Integar.MIN_VALUE;
		for (int x : array) {
			if (x < min) min = x;
			if (x > max) max = x;
		}

		System.out.printf("total is %d\n", total);
		System.out.printf("avg is %f\n", avg);
		

	}
}